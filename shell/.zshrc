# interactive login prompt
echo "interactive login zsh"

# DOTFILES_HOME=$HOME/.config/dotfiles

source ~/.env_init
source ~/.functions
source ~/.aliases

# HOST_SPECIAL=$DOTFILES_HOME/shell/$(hostname)/.zshrc
# [[ ! -f $HOST_SPECIAL ]] || source $HOST_SPECIAL

proxy


